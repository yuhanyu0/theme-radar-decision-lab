name: Repricing Experiment CI
on:
  pull_request:
permissions:
  contents: read
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"
          cache: pip
      - name: Install
        run: python -m pip install --upgrade pip && pip install -e '.[dev]'
      - name: Original suite
        run: pytest -q
      - name: Repricing experiment suite
        run: pytest -q experiments/repricing_graph_v0_1/test_*.py
      - name: Ruff experiment
        run: ruff check experiments/repricing_graph_v0_1/*.py
